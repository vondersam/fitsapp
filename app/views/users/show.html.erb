<%= content_for(:title) do %>
  Fitsapp | <%= @user.first_name + ' ' + @user.last_name %>
<% end %>

<div class="container">
  <div class="row">
    <div class="col-xs-12">
      <%= image_tag "#{@user.pic_url}", {class: "avatar-square-lg"} %>
    </div>
  </div>
  <div class="row">
    <div class="col-xs-12">
      <h2><%= @user.first_name %><%= " " %><%= @user.last_name %></h2>
      <p><%= @user.role %></p>
    </div>
  </div>
</div>


<%# if @user.plan.any? %>

    <%#= @user.plan.title %>
    <%# end %>
